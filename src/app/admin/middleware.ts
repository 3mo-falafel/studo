// This middleware is no longer used - authentication is handled by the root middleware.ts
// with cookie-based session management.
export function middleware() { 
  // This file can be removed
}
export const config = { matcher: [] }
